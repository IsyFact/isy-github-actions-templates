name: OSS Review Toolkit
on:
  workflow_call:

jobs:
  ort:
    runs-on: ubuntu-latest
    steps:
      - name: Use HTTPS instead of SSH for Git cloning
        run: git config --global url.https://github.com/.insteadOf ssh://git@github.com/
      - name: Checkout project
        uses: actions/checkout@v4
      - name: Download report template
        run: |
          curl -o cve_summary_report_template.ftl https://raw.githubusercontent.com/IsyFact/isy-github-actions-templates/v1.2.0/ort-config/cve_summary_report_template.ftl
      - name: Download license management config files
        run: |
          curl -o license-classifications.yml https://raw.githubusercontent.com/IsyFact/isy-github-actions-templates/mariusarltmsg-patch-1/ort-config/license-classifications.yml
          curl -o evaluator.rules.kts https://raw.githubusercontent.com/IsyFact/isy-github-actions-templates/mariusarltmsg-patch-1/ort-config/evaluator.rules.kts
      - name: Run GitHub Action for ORT
        uses: oss-review-toolkit/ort-ci-github-action@v1
        with:
          docker-cli-args:
            --mount type=bind,source=${{ github.workspace }},target=/home/ort/.ort/templates/
            --mount type=bind,source=${{ github.workspace }},target=/home/ort/.ort/config/
          report-formats: WebApp,StaticHtml,HtmlTemplate -O HtmlTemplate=template.path=/home/ort/.ort/templates/cve_summary_report_template.ftl
      - name: Download workflow artifacts
        uses: actions/download-artifact@v4
        with:
          path: ${{ github.workspace }}/artifacts
      - name: Log artifacts
        run: |
          ls -R ${{ github.workspace }}/artifacts
  upload_artifact:
    needs: ort
    runs-on: ubuntu-latest
    steps:
      - name: Upload the artifact
        id: upload
        uses: actions/upload-artifact@v4
        with:
          name: AsciiDoc_cve_summary_report_template.adoc
          path: ${{ github.workspace }}/artifacts
      - name: Deploy to target repository
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ${{ github.workspace }}/artifacts
          destination_repo: IsyFact/isy-github-actions-templates  # Ziel-Repository angeben
#          user_name: ''
#          user_email: 'actions@github.com'
          keep_files: true